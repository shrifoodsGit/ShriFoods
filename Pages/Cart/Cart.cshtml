@page
@model ShriFoods.Pages.Cart.CartModel
@{
}
<div class="container-fluid">

    <div class="container">
        <h3>Your Cart items </h3>
        <br />
        <table class="table table-striped">
            <thead>
                <tr class="table-hover">
                    <th scope="col">ProductName</th>
                    <th scope="col">Weight</th>
                    <th scope="col">Qty</th>
                    <th scope="col">Price</th>
                    <th scope="col">Total</th> 
                </tr>
            </thead>
            <tbody>
                @foreach (var cartItems in Model.only_CartModel)
                {
                    var rowtotal = cartItems.Quantity* cartItems.Price;

                    <tr>
                        <td>@cartItems.ProductName</td>
                        <td>@cartItems.ProductWeight</td>
                        <td>@cartItems.Quantity</td>
                        <td>@cartItems.Price</td>                   
                        <td>@rowtotal.ToString()</td>
                        <td>
                            <!-- Delete form targeting the 'OnPostDelete' handler -->
                            <form method="post" asp-page-handler="Delete" asp-route-id="@cartItems.CartId">
                                <button type="submit" class="btn btn-danger"
                                        onclick="return confirm('Are you sure you want to delete this?')">
                                    Delete
                                </button>
                            </form>
                        </td>
                        <td>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <br />
        <div class="row">
            <button class="col-6">
                <a href="/Index" class=""><input type="button" class="btnSubmit" value="Continue Shopping" /></a>
            </button>
            <button class="col-6" >
                <a href="/Order/CheckOut" class=""><input type="button" class="btnSubmit" value="CheckOut Now" /></a>
            </button>
        </div>
       
    </div>

</div>
